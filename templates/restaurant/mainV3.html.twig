{% block restoV3 %}
<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top" data-navbar-on-scroll="data-navbar-on-scroll">
        <div class="container"><a class="navbar-brand d-inline-flex" href="{{ path('app_index') }}"><img class="d-inline-block" src="{{ asset('assets/img/gallery/logo.svg') }}" alt="logo" /><span class="text-1000 fs-3 fw-bold ms-2 text-gradient">Uber Manger</span></a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"> </span></button>
          <div class="collapse navbar-collapse border-top border-lg-0 my-2 mt-lg-0" id="navbarSupportedContent">
            <div class="mx-auto pt-5 pt-lg-0 d-block d-lg-none d-xl-block">
              <p class="mb-0 fw-bold text-lg-center">Deliver to: <i class="fas fa-map-marker-alt text-warning mx-2"></i>Limoges<span class="fw-normal"> </span><span></span></p>
            </div>
            <form class="d-flex mt-4 mt-lg-0 ms-lg-auto ms-xl-0">
              
              
            </form>
            {% if is_granted('ROLE_ADMIN') %}
              <a class="btn btn-white shadow-warning text-warning" href="{{ path('admin') }}" type="submit"> <i class="fas fa-user me-2"></i>admin</a>
              {% endif %}
            {% if not is_granted("ROLE_USER") %}
              <a class="btn btn-white shadow-warning text-warning" href="{{ path('app_login') }}" type="submit"> <i class="fas fa-user me-2"></i>Login</a>
              <a class="btn btn-white shadow-warning text-warning" href="{{ path('app_register') }}" type="submit"> <i class="fas fa-user me-2"></i>Register</a>
              {% endif %}
              {% if is_granted('ROLE_USER') %}
              <a class="btn btn-white shadow-warning text-warning" href="{{ path('app_profile') }}" type="submit"> <i class="fas fa-user me-2"></i>Profil</a>
              <a class="btn btn-white shadow-warning text-warning" href="{{ path('app_logout') }}" type="submit"> <i class="fas fa-user me-2"></i>Log out</a>
              {% endif %}
          </div>
        </div>
</nav>

{% endblock  %}